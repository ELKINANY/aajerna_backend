# Aajerna Backend 🌙

هذا هو المشروع الخلفي (Backend) لتطبيق "أجرنا"، وهو عبارة عن خادم (Server) مبني باستخدام **Express.js** يعمل كوسيط (Proxy) لجلب الأحاديث النبوية من Hadith API.

## 🚀 المميزات

- خادم سريع وخفيف باستخدام Express.js.
- تفعيل CORS للتعامل مع الطلبات من الواجهة الأمامية (Frontend).
- نظام Proxy لجلب البيانات من `hadithapi.com` باستخدام مفتاح API مخفي.

## 🛠 التكنولوجيات المستخدمة

- **Node.js**: بيئة تشغيل JavaScript.
- **Express**: إطار عمل لبناء الخادم.
- **Axios**: لإرسال طلبات HTTP إلى API الخارجي.
- **Dotenv**: لإدارة متغيرات البيئة (مفتاح API).
- **CORS**: للسماح بالطلبات من نطاقات مختلفة.

## 📥 التثبيت والتشغيل

### 1. المتطلبات

تأكد من تثبيت **Node.js** على جهازك.

### 2. تنزيل التبعيات

قم بتشغيل الأمر التالي في المجلد الرئيسي للمشروع:

```bash
npm install
```

### 3. إعداد متغيرات البيئة

قم بإنشاء ملف باسم `.env` في المجلد الرئيسي وأضف فيه مفتاح API الخاص بك:

```env
HADITH_API_KEY=your_api_key_here
```

> [!IMPORTANT]
> يمكنك الحصول على مفتاح API من [hadithapi.com](https://hadithapi.com).

### 4. تشغيل المشروع

لتشغيل المشروع في بيئة التطوير (مع التحديث التلقائي):

```bash
npm run dev
```

لتشغيل المشروع بشكل عادي:

```bash
npm start
```

سيعمل الخادم بشكل افتراضي على: `http://localhost:5000`

## 📡 روابط API

### جلب الأحاديث

- **الرابط**: `GET /api/hadiths`
- **المعاملات**:
  - `page` (اختياري): رقم الصفحة المراد جلبها.
- **مثال**: `http://localhost:5000/api/hadiths?page=1`

## 📞 التواصل

إذا كان لديك أي استفسار، لا تتردد في التواصل معي.
